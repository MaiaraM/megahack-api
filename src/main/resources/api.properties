spring.jackson.mapper.default-view-inclusion=true
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect

# evitar essa configuracao ativa em producao
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update

spring.jackson.default-property-inclusion=non_null

# suporte a jdbc e redis
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always

spring.datasource.url=jdbc:postgresql://localhost:5432/apimegahack
spring.datasource.username=postgres
spring.datasource.password=postgres
#
#spring.datasource.url=${JDBC_DATABASE_URL}
#spring.datasource.username=${JDBC_DATABASE_USERNAME}
#spring.datasource.password={JDBC_DATABASE_PASSWORD}

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB


# jwt
# 10 dias em milis
jwt.expiration_time=860000000
# secret key
jwt.secret=45152ebe-807a-11e9-b073-83e32af30ba7





